fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt explicitly. passlib 1.7.4 (2020) reads bcrypt.__about__, removed in
# bcrypt 4.1+, and fails outright against 5.x — which made every hash and verify
# raise on a fresh install while the pinned local env kept working.
bcrypt==4.0.1
python-dotenv==1.0.1
pydantic[email]==2.9.2
python-multipart==0.0.12
